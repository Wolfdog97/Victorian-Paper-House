using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * Allow object to be moved to a position
 * Option 1: Select item and location
 * Option 2: Select item, bring up tab
 *
 * Could this all be handled with in the inpect script of props have enums? 
 */
public class Movable : MonoBehaviour
{
	public Transform[] validLocations;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
